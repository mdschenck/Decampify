# Secrets
.env
.env.local
.env*.local
# Write-scoped R2 token used only by scripts/upload-to-r2.mjs — never commit
.env.upload

# Deps / build
node_modules/
.vercel/

# Audio masters staged locally before R2 upload (never commit large media)
/_uploads/
*.wav
*.flac
*.aiff

# OS / editor
.DS_Store
Thumbs.db
